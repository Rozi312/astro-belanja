[providers]
providers = ["php"]

[variables]
PHP_VERSION = "8.3"

[phases.setup]
nixPkgs = ["...", "php83", "php83Packages.composer"]

[phases.install]
cmds = ["composer install --no-dev --no-interaction --prefer-dist", "npm install && npm run build"]